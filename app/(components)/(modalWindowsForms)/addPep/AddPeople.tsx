import withModalWindow from '@/app/modules/ModalWindow/withModalWindow';
import React from 'react';

type PropsType = {
    closeWindow: any
}

function AddPeople(props: PropsType) {
    return (
        <div>
            
        </div>
    );
}

export default withModalWindow(AddPeople);